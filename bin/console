#!/usr/bin/env php
<?php
require __DIR__ . '/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use MyCommands\Command\HelloCommand;

$app = new Application('My Commands', '1.0.0');
// registre todos os comandos
$app->add(new HelloCommand());
// execute
$app->run();
